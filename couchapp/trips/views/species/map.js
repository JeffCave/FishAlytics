/**
 * Species Maps
 * 
 * Within our catches we record the species caught.
 * 
 */
function(doc) {
	// code views/lib/catch.js
//	if(!catchUtils.isTrip(doc)){
//		return;
//	}
//	
//	$catches =catchUtils.asCatches(doc); 
//	for($c in $catches){
//		emit($catches[$c].key,$catches[$c].val); 
//	}
	
}
