before_script:
  - apt-get update -qq && apt-get install -y -qq nodejs npm couchdb
  - npm install 
  - npm run start:c9

after_script:
  - npm run stop:c9

build:
  script:
    - grunt couch

deploy:
  script:
    - grunt couch-deploy
